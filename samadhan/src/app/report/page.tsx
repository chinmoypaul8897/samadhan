import { CaptureFlow } from "@/components/report/CaptureFlow";

// Full-focus capture flow (AppShell hides the nav chrome on /report*). C2.
export default function ReportPage() {
  return <CaptureFlow />;
}
